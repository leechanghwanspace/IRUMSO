package com._roomthon.irumso.user.suveyInfo;

import org.springframework.data.jpa.repository.JpaRepository;

public interface SurveyRecommendationRepository extends JpaRepository<SurveyRecommendation, Long> {
}